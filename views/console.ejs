<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/png" href="/img/favicon.png" />
    <link rel="stylesheet" href="css/console.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Console</title>
</head>

<body>

    <div class="container my-4">
        <h1 class="title"><a class="secret-link" href="../">Console</a></h1>
        <table class="table table-striped table-responsive">
            <thead>
                <tr>
                    <th>Url</th>
                    <th>Code</th>
                    <th>Date</th>
                    <th>Visits</th>
                    <th>Edit</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <% for(let i = 0; i < allDocs.length; i++) { %>
                    <tr>
                        <td><%= allDocs[i].longUrl %></td>
                        <td>
                            <a href="<%= allDocs[i].shortUrl %>" rel="noopener noreferrer" target="_blank"><span class="urlText"><%= allDocs[i].shortUrl %></span></a>
                            <input type="text" class="urlInput" value="<%= allDocs[i].shortUrl %>" style="display: none;">
                        </td>
                        <td><%= allDocs[i].formatedDate %></td>
                        <td><%= allDocs[i].visits %></td>
                        <td>
                            <button class="editButton">✏️</button>
                            <button class="saveButton" style="display: none;">💾</button>
                        </td>
                        <td>
                            <form action="/delete/<%= allDocs[i].shortUrl %>" method="post">
                                <button type="submit" class="deleteLink">❌</button>
                            </form>
                        </td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
    <script src="/js/console.js"></script>
</body>

</html>